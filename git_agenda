Why source control?
- Backup
- Maintain History
- Undo Changes
- Collaboration
- Comparing

1) Git installation
2) Basic commands
3) Creating Branch
4) Merging
5) Resolving conflicts

Global config:
git config --global user.name "Name"
git config --global user.email "Email"
git config --global -l


Git basic commands:
git clone(with SSH and https)
windows ssh - puttygen
mac ssh - ssh-keygen
git status
git add (staging area)
git commit -m
git push -u origin master
git pull origin master

test